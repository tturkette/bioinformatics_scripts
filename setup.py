from setuptools import setup, find_packages

setup(
    name='bioinformatics_scripts',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your project dependencies here
    ],
    test_suite='tests',
)